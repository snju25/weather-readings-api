import {Router } from "express"
// import { getAllReadings } from "../models/readings.js"

const readingsRouter = Router()

// readingsRouter.get("/readings",getAllReadings)

export default readingsRouter